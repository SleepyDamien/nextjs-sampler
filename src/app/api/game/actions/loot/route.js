// api/game/actions/loot

// Simulate a database of loot
export async function GET(request) {

    const req = request.json();
    // Add loot here
    const gold = 0;
    const junk = [];

    res.status(200).json(player);
}